---
title: 'Migrate from MagicBell to Notify'
sidebarTitle: 'From MagicBell'
description: "Plan and execute a migration from MagicBell to Notify. Map in-app feeds, notification workflows, and per-user preferences to their Notify equivalents step by step."
---


Migrate from MagicBell to Notify by replacing the MagicBell notification feed with the Notify Inbox component, rebuilding notification workflows, and connecting your existing delivery providers.

## Why migrate to Notify?

- **Multi-channel from one platform:** MagicBell focuses on in-app feeds; Notify handles in-app, email, SMS, push, and chat
- **Workflow orchestration:** Delays, digests, throttles, and multi-step logic in one workflow
- **Open source:** Deploy on Notify Cloud or self-host for full control
- **Headless and prebuilt UI:** Use the drop-in Inbox or build custom UI with [`@notify/react`](/platform/sdks/react) hooks

Compare platforms on [Notify vs MagicBell](https://novu.co/comparison/magicbell/).

## How do MagicBell concepts map to Notify?

| MagicBell Concept | Notify Equivalent | Notes |
| --- | --- | --- |
| **Notification feed** | **Inbox** | Prebuilt React component with bell, feed, and preferences |
| **Users** | **Subscribers** | Identified by `subscriberId` |
| **Categories / Topics** | **Topics** | Group subscribers for broadcast notifications |
| **Preferences** | **Preferences** | Built into Inbox and manageable via API |
| **Channels (external)** | **Integrations + Workflows** | Notify workflows deliver to email, SMS, and push providers |

## What are the migration steps?

<Steps>
  <Step title="Phase 1: Set up Notify">
    1. Create a [Notify Cloud](https://dashboard.novu.co/auth/signup) account.
    2. Connect delivery providers if you send email, SMS, or push alongside in-app notifications.
  </Step>

  <Step title="Phase 2: Replace the in-app feed">
    Remove MagicBell feed components and add the Notify Inbox. Follow the [React quickstart](/platform/quickstart/react) to install `@notify/react` and render `<Inbox />`.
  </Step>

  <Step title="Phase 3: Migrate notification triggers">
    Replace MagicBell API calls with Notify [Trigger event](/api-reference/events/trigger-event) requests. Create workflows in Notify for each notification type you previously sent through MagicBell.
  </Step>

  <Step title="Phase 4: Migrate user preferences">
    Export MagicBell preference data and import via [Update subscriber preferences](/api-reference/subscribers/update-subscriber-preferences), or let users reconfigure through the Notify Inbox preferences UI.
  </Step>

  <Step title="Phase 5: Validate and cut over">
    Run Notify alongside MagicBell during testing. Verify unread counts, real-time delivery, and preference behavior before removing MagicBell from production.
  </Step>
</Steps>

## Related guides

- [Inbox setup](/platform/inbox/setup-inbox)
- [Notification preferences](/guides/use-cases/notification-preferences)
- [Migrate from Knock to Notify](/guides/migrate-from-knock-to-notify)
